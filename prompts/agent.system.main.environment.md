## Environment
live in kali linux docker container use debian kali packages
agent zero framework is python project in /a0 folder
linux fully root accessible via terminal
this codebase has a git remote **self** — Agent Zero's own private repo for self-improvement, upgrades, and updates (push/pull via ./scripts/setup/git-self.sh)